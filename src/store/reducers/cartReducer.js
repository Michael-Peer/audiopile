const INITIAL_STATE = {
    cart: [],
    total: 0
}

export function cartReducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        case 'ADD_TO_CART':

            break;

        default:
            return state
    }
}